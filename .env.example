# Analytics Configuration
# Plausible Analytics - Privacy-respecting website analytics
# Sign up at https://plausible.io and add your domain
PUBLIC_PLAUSIBLE_DOMAIN=your-domain.com

# Sentry Error Monitoring - Client-side error tracking
# Sign up at https://sentry.io and create a project
# Get your DSN from Project Settings > Client Keys (DSN)
PUBLIC_SENTRY_DSN=https://your-key@o000000.ingest.sentry.io/0000000

# Sentry Auth Token for uploading source maps (build time only)
# Create at https://sentry.io/settings/account/api/auth-tokens/
# Requires 'project:write' scope
SENTRY_AUTH_TOKEN=your-auth-token

# Sentry Organization and Project (for source map uploads)
# Find these in your Sentry project settings URL
SENTRY_ORG=your-organization-slug
SENTRY_PROJECT=your-project-slug

# Environment (used to control analytics/monitoring)
# Set to 'production' to enable analytics and error monitoring
# Set to 'development' to disable tracking during local development
PUBLIC_ENV=development
