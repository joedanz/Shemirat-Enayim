# Shemirat Enayim - Environment Variables Example
# Copy this file to .env and fill in your values

# =============================================================================
# Analytics (Optional - Plausible Analytics)
# =============================================================================
# If you want to use Plausible Analytics for page view tracking:
# PUBLIC_PLAUSIBLE_DOMAIN=shemiratenayim.org
# PUBLIC_ENV=production

# =============================================================================
# Error Monitoring (Optional - Sentry)
# =============================================================================
# Sentry is DISABLED by default. To enable it, you must configure ALL of these:

# Sentry DSN (from your Sentry project settings)
# PUBLIC_SENTRY_DSN=https://your-dsn@sentry.io/your-project-id

# Sentry Auth Token (for uploading source maps during build)
# Get from: https://sentry.io/settings/account/api/auth-tokens/
# SENTRY_AUTH_TOKEN=your_auth_token_here

# Sentry Organization Slug
# SENTRY_ORG=your-org-slug

# Sentry Project Name
# SENTRY_PROJECT=your-project-name

# =============================================================================
# Build Configuration
# =============================================================================
# Environment (development, staging, production)
# PUBLIC_ENV=development

# =============================================================================
# Notes
# =============================================================================
# - Sentry will ONLY be enabled if ALL four Sentry variables are configured
# - If any Sentry variable is missing, Sentry will be completely disabled (no warnings)
# - Analytics only runs when PUBLIC_ENV=production
# - Prefix public variables with PUBLIC_ to make them available in the browser
# - Keep your actual .env file out of version control (already in .gitignore)
